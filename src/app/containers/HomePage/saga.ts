// import { call, put, takeLatest } from 'redux-saga/effects';
// import { actions } from './slice';

export function* homePageSaga() {
  // yield takeLatest(actions.setCurrentUser.type, getProfileData);
}
